<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/libraryFragment">
    <fragment
        android:id="@+id/libraryFragment"
        android:name="com.ongyx.cassette.ui.LibraryFragment"
        android:label="LibraryFragment">
        <action
            android:id="@+id/action_playlistFragment_to_placeholder"
            app:destination="@id/placeholder" />
        <action
            android:id="@+id/action_playlistFragment_to_placeholder2"
            app:destination="@id/placeholder2" />
    </fragment>
    <fragment android:id="@+id/placeholder" />
    <fragment android:id="@+id/placeholder2" />
</navigation>